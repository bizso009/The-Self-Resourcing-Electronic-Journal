jQuery.ajaxSetup({
            dataFilter: function(data, dataType) {
                return data;  
            }
        });

$(document).ready(function() {
	   // put all your jQuery goodness in here.
});